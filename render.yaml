services:
  - type: web
    name: license-server
    runtime: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn SERVER:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    envVars:
      - key: MAINTENANCE
        value: "0"
