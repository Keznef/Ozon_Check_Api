[project]
name = "license-server"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.115.5",
    "uvicorn[standard]==0.32.0",
    "pydantic==2.9.2",
    "requests==2.32.3",
]

[tool.pyinstaller]
name = "LicenseClient"
entry = "client.py"
